import { DidProvider } from './DidProvider';
export declare let didProvider: DidProvider;
export declare const assertDidProvider: (fields: Array<keyof DidProvider>) => void;
export declare const setDidProvider: (provider: DidProvider) => void;
